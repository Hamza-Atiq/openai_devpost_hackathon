[project]
name = "crickops-api"
version = "0.1.0"
description = "CrickOps AI FastAPI service"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi==0.116.1",
]

[dependency-groups]
dev = [
  "pytest==8.4.1",
  "ruff==0.12.4",
]

[tool.uv]
package = false
